name: LocalDictate
options:
  bundleIdPrefix: com.localdictate
  deploymentTarget:
    macOS: "13.0"
packages:
  ArgmaxOSS:
    url: https://github.com/argmaxinc/argmax-oss-swift.git
    from: 0.9.0
targets:
  LocalDictate:
    type: application
    platform: macOS
    sources:
      - path: LocalDictate
    dependencies:
      - package: ArgmaxOSS
        product: WhisperKit
    settings:
      base:
        PRODUCT_NAME: LocalDictate
        PRODUCT_BUNDLE_IDENTIFIER: com.localdictate.app
        MARKETING_VERSION: "0.1"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "5.0"
        ENABLE_HARDENED_RUNTIME: YES
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: "2LCM5MU5MF"
        CODE_SIGN_ENTITLEMENTS: LocalDictate/LocalDictate.entitlements
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_CFBundleDisplayName: LocalDictate
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.utilities
        INFOPLIST_KEY_NSHumanReadableCopyright: ""
        INFOPLIST_KEY_NSMicrophoneUsageDescription: "LocalDictate necesita acceso al micrófono para grabar tu voz y transcribirla localmente en esta Mac."
        MACOSX_DEPLOYMENT_TARGET: "13.0"
        ASSETCATALOG_COMPILER_APPICON_NAME: ""
    scheme:
      testTargets:
        - LocalDictateTests
  LocalDictateTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: LocalDictateTests
    dependencies:
      - target: LocalDictate
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.localdictate.app.tests
        GENERATE_INFOPLIST_FILE: YES
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: "2LCM5MU5MF"
        MACOSX_DEPLOYMENT_TARGET: "13.0"
